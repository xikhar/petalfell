# Retired authored data

This directory preserves authored inputs that no current runtime loads.

`legacy-world/world.json` is the former 3,456-block circular fixture topology.
Its active replacement is the production atlas topology registered by
`content/chapter_01/atlas.json`.
