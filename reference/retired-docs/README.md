# Retired documentation

These snapshots preserve superseded plans, implementation descriptions and
verification records. They are historical evidence only; links, commands and
status claims inside them may be stale.

Current project authority remains at the repository root and under `docs/`.
Do not copy an archived method back into current documentation without a new
decision and current runtime evidence.

`2026-09-02-pre-cleanup/` records the final multi-pipeline documentation before
the project standardized on the accepted map-guided production runtime.
